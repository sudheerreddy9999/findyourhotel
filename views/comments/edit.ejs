<%- include("../partials/header") %>


    <div class="container">
        <div class="row">
            <h1 style="text-align: center;">Edit your comment on
                <%= camp.name %>
            </h1>
            <div style="width:30%; margin:30px auto;">
                <form action="/Hotels/<%= camp._id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
                    <div class="form-group">
                        <input class="form-control" type="text" name="comment[text]" value="<%= comment.text %> ">
                    </div>

                    <div class="form-group">
                        <button class="btn btn-block btn-lg btn-success" type="submit">Submit! </button>
                    </div>
                </form>
                <a href="/Hotels">Go Back</a>
            </div>
        </div>
    </div>

    <%- include("../partials/footer") %>